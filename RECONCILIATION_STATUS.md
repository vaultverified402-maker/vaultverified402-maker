# Production reconciliation

This branch is reserved for recovering the current Vercel production frontend into GitHub before restoring Git-based deployments.

Production must not be overwritten until preview parity is verified.
